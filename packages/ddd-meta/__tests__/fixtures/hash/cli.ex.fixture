defmodule NvencLab.CLI do
  @moduledoc """
  Locates the `nvenc-cli` binary and translates an options map into its argv.
  The argv/flag names mirror the contract in `tools/nvenc-lab/README.md`.
  """

  @doc """
  Resolve the `nvenc-cli` executable. Order: `NVENC_CLI` env override, then the
  release build under `direct-sdk/rust/target/release/nvenc-cli`, then `$PATH`.
  """
  @spec binary() :: {:ok, String.t()} | {:error, :not_found}
  def binary do
