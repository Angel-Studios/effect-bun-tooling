package day.harvest.admin

import day.harvest.admin.auth.AdminAccessTokenVerifier
import day.harvest.admin.auth.AdminJwtConfig
import day.harvest.admin.auth.buildAdminAccessTokenVerifier
import io.ktor.server.engine.embeddedServer
import io.ktor.server.netty.Netty
import org.slf4j.Logger
import org.slf4j.LoggerFactory

/** Default admin HTTP port when neither `PORT` nor `ADMIN_HTTP_PORT` is set. */
private const val DEFAULT_ADMIN_PORT: Int = 8081
