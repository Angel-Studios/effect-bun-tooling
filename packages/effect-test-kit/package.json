{
  "name": "@packages/effect-test-kit",
  "version": "0.2.1",
  "description": "Cast-free, framework-agnostic test assertions for Effect tagged errors. expectTag / expectFailureTag / expectCauseFailureTag / expectLeftTag narrow a failure to a specific tagged-error member by its `_tag` discriminant, replacing `value as SpecificError` casts and `x instanceof SchemaError` guards with one typed helper that throws loudly on any mismatch, so a converted test still asserts.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Angel-Studios/effect-bun-tooling.git",
    "directory": "packages/effect-test-kit"
  },
  "homepage": "https://github.com/Angel-Studios/effect-bun-tooling/tree/main/packages/effect-test-kit",
  "type": "module",
  "exports": {
    "./tagged": "./src/tagged.ts"
  },
  "files": [
    "src",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "bun": ">=1.3.12"
  },
  "scripts": {
    "tsc": "tsc --noEmit",
    "lint:effect": "effect-tsgo diagnostics --project tsconfig.json",
    "check": "biome check src/ __tests__/",
    "test:unit:once": "bun test"
  },
  "peerDependencies": {
    "effect": "4.0.0-rc.109"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "effect": "catalog:",
    "typescript": "catalog:"
  }
}
